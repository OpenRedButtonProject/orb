set (autostart ${PLUGIN_ORB_AUTOSTART})
set (preconditions Platform)

map()
  kv(privateComRpc ${PLUGIN_ORB_PRIVATE_COMRPC})
  key(root)
  map()
    kv(outofprocess true)
    kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
  end()
end()
ans(configuration)
